int main() {
// return fact(0);

int i = 3, result = 2;
int come = 1;
while((!(i < 0))) {
    result = (result * 2);
    if((come & true)) {
        come = 0;
    } else {
        come = 1;
        //result = (result - 1);
    }
    i = (i - 1);
}

return result;
}

int fact(int n) {
if (((n | false))) return 16;
else return 0;
}